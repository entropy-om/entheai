local wezterm = require 'wezterm'

-- entheai — minimalist branded window (generated by the launcher; do not edit).
-- Mirrors ghostty-minimal.conf: same palette, padding, and rain-on-glass shader.

local config = {}

config.colors = {
  background = '#0f0e1d',
  foreground = '#e1cba6',
}
config.font = wezterm.font('JetBrainsMono Nerd Font')
config.window_padding = {
  left = 14,
  right = 14,
  top = 10,
  bottom = 10,
}
config.scrollback_lines = 100000
config.background_opacity = 1.0

-- Rain-on-glass shader. WezTerm's custom-shader config key is `shaders`, a
-- table with a `frag` path (and optional `vert`); the path is rewritten on
-- materialize. See https://wezterm.org/config/lua/config/shaders.html
config.shaders = {
  frag = '{{SHADER}}',
}

return config
