[package]
name = "ternary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Native ternary (BitNet b1.58) matrix support — ayeOS loading, packed 2-bit decode, pure-Rust ternary GEMM (Phase 1, no entheai wiring)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
rayon.workspace = true
