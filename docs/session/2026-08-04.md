# entheai — Session Summary (2026-08-04, afternoon)

Continuity handoff. Everything below is committed or scaffolded and verified
live unless marked pending. Focus: ops on the M1 + two book scaffolds through
the entheai fan-out pipeline.

## 1. Babilon: max-one + breathing room (watchdog)

Two Babilon-family processes were pegging ~100% CPU each. Shipped a
launchd watchdog that is now permanent:

- **`~/scripts/babilon-watchdog.sh`** — enforces max 1 Babilon process
  (kills duplicates, keeps the longest-running) and duty-cycles the survivor
  with SIGSTOP/SIGCONT: 30s on / 30s off (tunable via
  `BABILON_ON_SECS`/`BABILON_OFF_SECS` in the plist) → ~50% average CPU with
  the window still open.
- **`~/Library/LaunchAgents/dev.peterlodri.babilon-watchdog.plist`** —
  loaded, runs every 15s.
- macOS notes: no `flock`, no `pgrep -fc`, no `mapfile`, no `ps -o etimes`
  (bash 3.2 / BSD ps) — all handled with compatible equivalents.
- Babilon app polish: `HugWaveView`'s `TimelineView(.animation)` (the
  60/120Hz CPU burner, ~100 path segments/frame × 4 waves) throttled to
  `minimumInterval: 1.0/20` — 20fps cap. Built + relaunched.
- `OM MANI PADME HUNG` added to the app footer (gold mantra line) and the
  whisper rotation ("OM MANI PADME HUNG… hummm…").

## 2. art.vaked.dev — entheai Visual Quantum Gallery (scaffold → e2e → live)

- New repo **`peterlodri-sec/art.vaked.dev`** (public, pushed to main) —
  the gallery is now part of the backyard-ultra hourly sync loop (69 repos).
- `index.html` = the 42-artifact gallery (adapted from `entheai/public/gallery.html`,
  canonical art.vaked.dev), plus a **Live Demos** section linking the
  interactive sphere-mesh / recursive-loop / hyper-mesh / 1-bit-mesh /
  generative-canvas pieces.
- **Push-to-deploy CI**: `.github/workflows/deploy.yml` → Cloudflare Pages
  project `art-vaked-dev` (secrets already in the repo); first push deployed.
- Live + verified: all paths 200, demos section present.
- Old single-canvas page preserved as `generative.html`.

## 3. backyard-ultra.sh polish

- Babilon build step pointed at the real project
  (`babylon-ultra-dict/BABYLON-ultra-dict.xcodeproj` — the old
  `quant-tiktok/Babilon.xcodeproj` no longer exists); the hourly build now
  actually runs (`** BUILD SUCCEEDED **` in the log).
- **Pause flag**: `/tmp/babilon-pause` (auto-expires 30 min after creation)
  makes the script skip the final `open` — used to "close Babilon for half
  an hour" while the app was being rebuilt.

## 4. Book scaffolds through entheai fan-out (entheai + agy + deepseek)

Established pattern: `pocoo.vaked.dev/book/<name>/scaffold.toml` —
`[book]` metadata + `[fanout]` executor config + `[[coder]]` chapter tasks,
fed to `entheai --fanout`. Two new scaffolds:

- **`book/humm-flow/`** — "HUMM FLOW", a sci-fi novel in forty-two trits
  (STATION VELVET, a six-syllable hum in the CMB, the ternary key, the door).
  43 coder tasks + integrator, `max_depth = 3`.
- **`book/chlo/`** — "ULTRA SENSITIVE", a book for Chlo 🎀, 42 pages,
  sensitive mode (soul soothing, the river). 15 coder tasks + integrator.

### Pipeline findings (important for future book runs)

- **DeepSeek status**: `default_model` + `[router].orchestrator` moved to
  `openrouter/deepseek/deepseek-chat` (the direct `DEEPSEEK_API_KEY` is not
  provisioned; OpenRouter was out of credits → 402). Configured as the
  brain, but **needs credits to actually run**.
- **HF-mac path (the working fallback)**: per "for HF use HF-mac", the
  orchestrator runs on a local Apple MLX model:
  `mlx_lm.server --model mlx-community/gemma-3-4b-it-4bit --port 1337`
  (16GB M1, local + private). Coders run via `agy --model gemini-3.6-flash-high`
  through entheai's `[fanout] executor = "agy"` (max recursion 3 = MAX_DEPTH
  guard in `crates/orchestrator/src/agy.rs`).
- **BUG FOUND + FIXED**: entheai's fan-out sends consecutive
  `["user","user"]` messages; the strict MLX server rejects them
  ("Conversation roles must alternate"). A small merging proxy
  (`/tmp/proxy.mjs`, :1338 → :1337) collapses consecutive same-role
  messages — the fan-out now runs end-to-end and writes chapters.
  *(TODO: fix upstream in entheai — send the orchestrator preamble as a
  `system` role or merge in `adk-rust` — instead of the proxy.)*
- E2E result for ULTRA SENSITIVE: 14 chapters written into worktrees by agy,
  integration merged the 42-page manuscript (42 "Page N of 42" markers, 41
  pagebreaks), **published and live**:
  https://pocoo.vaked.dev/demos/book/ultra-sensitive.html

## Running infra (as of session end)

- `mlx_lm.server` on 127.0.0.1:1337 (MLX Gemma 3 4b) — background.
- merging proxy on 127.0.0.1:1338 → 1337 — background.
- Babilon watchdog: launchd, every 15s.

## Pending / follow-ups

- DeepSeek credits (OpenRouter) for the "real" brain; the direct key.
- Fix the consecutive-role issue upstream in entheai (drop the proxy).
- HUMM FLOW full 42-chapter fan-out run (scaffold ready; needs credits or
  the local path).
