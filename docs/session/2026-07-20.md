# entheai — Session Summary (through 2026-07-20)

A continuity handoff. Everything below is **committed + green on `main`** (`cfebd78`, 281 tests) unless marked pending/deferred.

## What shipped this session

1. **Memory-brain handoff to Rahul.** memory-v1 engine (Tasks 0–7) was already done; the `--memory list/search/stats` inspection CLI landed (`af4461a`), and the remaining wiring (**Tasks 9–10: TUI + fan-out memory**) was **reassigned to @rahulmranga** — tagged via `.github/CODEOWNERS` (`/crates/memory/`) + inline `TODO(@rahulmranga)` markers at `tui::run` and `orchestrator::run_fanout` (`704512c`).

2. **Obsidian wiki-sync layer — new pillar, built end-to-end.** Brainstorm → spec (`88bc558`) → 12-task plan (`e47d2e6`) → subagent-driven build (`57c0374`→`6ea89a3`) → E2E-proven (seeds a temp vault with 53 files, writes nothing outside the managed subtree, silent no-op with no vault). New **`crates/obsidian`**: per-session live watcher mirrors repo docs + generates a wiki (docs mirror w/ wikilinks + copied assets, Architecture, `.remember/` sessions, indexes, Home MOC) into `<vault>/entheai-sync/`; FS-direct + best-effort MCP nudge; write-confinement adversarially proven. Config `[obsidian]`. See `docs/superpowers/specs/2026-07-19-entheai-obsidian-wiki-sync-design.md`.

3. **TUI-safe logging backend** (`db937f3`). `bin/entheai/src/logging.rs`: interactive TUI logs to `~/.cache/entheai/entheai.log` only (never corrupts the alt-screen); one-shot/`--fanout`/`--memory` mirror to stderr. Level via `ENTHEAI_LOG`/`RUST_LOG` (default `warn`). Fixes the previous silent-drop of every `log::` record.

4. **Whole-codebase review + polish + perf pass.** 6 parallel read-only reviewers + an opus hot-path complexity audit → **11 real bugs**, all fixed. Records: `docs/reviews/2026-07-20-whole-codebase-review.md` and the new **`docs/rust-style-and-performance.md`** (our own P1–P8 perf guide + reviewer checklist).
   - **External-input hardening:** providers OOM-cap + timeout (`ee292d6`); mcp handshake/request timeout + reader line cap (`6eca510`); tools shell streaming capped reader (kills child at cap) + file-read cap (`bb1fb98`).
   - **orchestrator worktree lifecycle** (`e9772bc`): keep unmerged branches (no lost work), skip integrating timed-out coders, clean up on every exit, reap pool.
   - **Hot-path complexity:** tui O(n²)/turn→O(Δ)/token + borrowed render (`bc08257`); companion glow LUT + `dt`-clock + QR (`fd49c8d`); core/providers trait now **borrows** (`38ea3a7`); obsidian `apply()` → `spawn_blocking` (`492610c`); core memory-context slot (`cfebd78`).
   - **Cleanup:** dead deps removed workspace-wide (`f8ec724`); stale `allow(deprecated)` dropped (`7858d01`).

5. **ultrawhale inspiration survey.** Cloned + surveyed `peterlodri-sec/ultrawhale` (the "main" inspiration — a mature Go agent). Portable patterns identified (see below).

## Current pillar/crate state

- **Workspace:** 20 crates + 3 bins, green (281 tests). Perf release profile tuned (LTO=fat, codegen-units=1, mimalloc). Hot files: `core`, `tui`, `providers`, `orchestrator`, `companion/render`, `bin/entheai/main.rs`.
- **memory** — engine done; **Tasks 9–10 wiring + 3 review findings are Rahul's** (see below).
- **obsidian** — done & green; standalone `entheai doctor` (write shader into a user's own Ghostty config) + Path-C ANSI fallback still a viz-Slice-2 follow-up.
- **viz** — Slice 1 (swarm) done; Slice 2 shader shipped via the native app.
- **native app** — `entheai.app` / `entheai --app` done (bundled minimalist Ghostty + raindrop shader).

## Pending / next steps

**Handed to @rahulmranga (his `crates/memory` — do NOT edit without coordinating):**
- memory-v1 **Tasks 9–10**: thread memory into `tui::run` + `orchestrator::run_fanout` (recipes in `docs/superpowers/plans/2026-07-19-entheai-memory-v1.md`).
- Review findings: **tri-store drift** (upsert w/ `embedding=None` NULLs `entries.embedding` but leaves the stale `vec_entries` row — move the `DELETE FROM vec_entries` out of the `if let Some(emb)` guard); + 2 minors (load_entries IN-clause cap; embed_batch index/length validation).

**Deferred (mine, follow-ups):**
- **obsidian incremental** — `apply()` still re-scans+re-renders the whole repo per debounced tick; thread the changed-path set / mtime-cache `SourceDoc`s (large; debounced ~500ms + now off-runtime, so low urgency).
- **viz** — standalone Slice 2 (`entheai doctor` + Path-C fallback); Slice 1 swarm refinements (HalfBlock, event-driven redraw, tachyonfx).

**ultrawhale ports (surveyed, not started) — ranked:**
- Tier 1: **durable sessions** (their `internal/session`: goal/todo/plan/mode state — this is entheai's deferred durable-sessions pillar, ready to lift), **policy + securefs** (scoped shell-segment approval + private-path enforcement — hardens `permission`), **compact** (context compaction — unblocks `/compress`), **repomap** (internal code map, could wrap `crabcc`).
- Tier 2: **evals** (replay harness — foundation for the dogfood loop), **blocks** (journaled atomic writes + rollback), **workflow** (budget-aware resumable JS workflows).

**Decisions (settled 2026-07-20):** the two flagged permissive defaults — `[permission].fanout_auto_approve = true` and the baked-in Sentry DSN — are **intentional, left as-is** per the maintainer.

## Context the next session needs

- **Shared multi-session `main` checkout.** Always: scoped `git add <exact paths>` (never `-A`/`-u`), commit, push immediately, `git pull --rebase` on non-FF. Never `git reset --hard`. An auto-stager hook can sweep other sessions' files — hence explicit pathspecs.
- **`entheai.toml` is in a PUBLIC repo** — no cleartext secrets. The Valyu MCP key lives in gitignored `.env` (`$VALYU_MCP_URL`).
- **rust-analyzer diagnostics lag badly here** — when they disagree with `cargo`, trust `cargo` (verified 4× this session that flagged "errors" were stale mid-edit indexing; the code compiled clean).
- **`snyk_code_scan`** (CLAUDE.md policy) is **unavailable** in this environment — note + proceed, don't block.
- **Workflow:** subagent-driven-development (fresh implementer per task + review), scoped commits. The **`docs/rust-style-and-performance.md`** guide is now the perf standard held to at review.
- **Obsidian:** project vault at `~/Library/Mobile Documents/iCloud~md~obsidian/entheai` (git-backed to `peterlodri-sec/entheai-vault`); collaborator **Rahul = GitHub `rahulmranga`**.
